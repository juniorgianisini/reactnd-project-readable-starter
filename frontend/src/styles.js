export default (theme) => ({
    post_paper: {
        padding: theme.spacing.unit * 2,
        textAlign: "left",
        color: theme.palette.text.secondary,
        margimTop: 10,
        marginBottom: 10
    },
 
    category_grid: {
        direction: "column",
        alignItems: "center",
        marginLeft: 20,
        marginRight: 20
    }
});